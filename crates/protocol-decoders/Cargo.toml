[package]
name = "protocol-decoders"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
packet-core = { path = "../packet-core" }

[dev-dependencies]
proptest = { version = "=1.11.0", default-features = false, features = ["std"] }

[lints]
workspace = true

[[bench]]
name = "link_layer_decode"
harness = false

[[bench]]
name = "network_layer_decode"
harness = false

[[bench]]
name = "transport_layer_decode"
harness = false

[[bench]]
name = "dns_message_decode"
harness = false
